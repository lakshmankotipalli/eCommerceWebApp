ecomm.factory('userFactory', ['HttpService', '$interpolate', function (HttpService, $interpolate) {

  var data = {
    login: 'data/data.json'
  };

  var factory = {};

  factory.userAuth = function (obj) {

  };

}]);
